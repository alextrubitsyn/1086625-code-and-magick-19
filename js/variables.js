'use strict';

(function () {
  var ESC_KEY = 'Escape';
  var ENTER_KEY = 'Enter';
  var TIMEOUT = 10000;
  var TIMEOUT_MESSAGE = 5000;

  window.variables = {
    ESC_KEY: ESC_KEY,
    ENTER_KEY: ENTER_KEY,
    TIMEOUT: TIMEOUT,
    TIMEOUT_MESSAGE: TIMEOUT_MESSAGE
  };
})();
